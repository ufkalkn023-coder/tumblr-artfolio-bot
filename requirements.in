#
# Çalışma zamanı doğrudan bağımlılıkları (insan tarafından düzenlenir).
# Bu dosya yetkili kaynaktır; requirements.lock bu dosyadan üretilir:
#   pip-compile requirements.in --output-file requirements.lock
#

pytumblr>=0.1.2
requests>=2.31.0
python-dotenv>=1.0.0
Pillow>=10.0.0
